[workspace]
members = ["."]
resolver = "2"

[package]
name = "scirust-elastic-latent-kv"
version = "0.1.0"
edition = "2021"
publish = false
rust-version = "1.89"
description = "Deterministic reference experiments for reconstruction-free latent KV attention"
license-file = "../../LICENSE.md"
repository = "https://github.com/Memorithm/scirust"

[lints.rust]
unsafe_code = "forbid"
missing_docs = "warn"
