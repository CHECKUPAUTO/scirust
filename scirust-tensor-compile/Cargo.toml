[package]
name = "scirust-tensor-compile"
version = "0.1.0"
edition = "2021"
publish = false
rust-version = "1.89"

[dependencies]
scirust-tensor-core = { path = "../scirust-tensor-core" }
scirust-tensor-ir = { path = "../scirust-tensor-ir" }
scirust-tensor-contraction = { path = "../scirust-tensor-contraction" }
