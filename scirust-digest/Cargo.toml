[package]
name = "scirust-digest"
version = "0.1.0"
edition = "2021"
rust-version = "1.89"
publish = false
description = "SciRust deterministic domain-separated content digests for persistence and provenance"

[dependencies]
sha2 = "0.10"
