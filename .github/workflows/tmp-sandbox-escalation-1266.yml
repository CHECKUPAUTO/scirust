name: tmp-sandbox-escalation-1266-replay

on:
  workflow_dispatch:

jobs:
  inert:
    runs-on: ubuntu-latest
    steps:
      - run: |
          python3 - <<'PY'
          assert True
          PY

          cargo +nightly-2026-07-02 fmt --all
