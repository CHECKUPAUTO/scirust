[package]
name = "tensor-parity-tooling"
version = "0.1.0"
edition = "2021"
publish = false
description = "SciRust tensor parity coverage matrix generator (standalone; NOT part of the scirust workspace build)"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "0.8"

[workspace]