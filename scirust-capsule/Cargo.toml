[package]
name = "scirust-capsule"
version = "0.1.0"
edition = "2024"
publish = false
rust-version = "1.89"
description = "Deterministic .scicap container encoding and payload integrity verification for SciRust"

[dependencies]
scirust-capsule-schema = { path = "../scirust-capsule-schema" }
serde_json = "1"
sha2 = "0.10"
